import React from "react";
import { Button } from "@material-ui/core";

const Home: React.FC = () => {
  return (
    <div>
      <Button color="secondary" variant="contained">
        Entrar
      </Button>
    </div>
  );
};

export default Home;
